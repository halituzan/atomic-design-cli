import React from "react";
import "./button.scss";
type Props = { text: string | undefined };

const Text = ({ text }: Props) => {
  return (
    <p className='text-slate-950 dark:text-white text-base md:text-lg '>
      {text}
    </p>
  );
};

export default Text;
